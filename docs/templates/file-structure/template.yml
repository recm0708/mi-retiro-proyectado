# Configuración de ejemplo.
# Propósito: describir el objetivo permanente del archivo YAML.
# Alcance: indicar qué automatización, formulario o configuración controla.

name: ejemplo

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validar:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar ejemplo
        run: echo "Validación de ejemplo"
